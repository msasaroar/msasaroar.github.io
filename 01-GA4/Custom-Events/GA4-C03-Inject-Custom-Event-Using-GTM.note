===============================Q2ChatGPT
html পেইজ এর কোড এ কোন পরিবর্তন না করে একটি পেইজ এর একটি button এর ক্লিকের ফলে 
একটি Custom HTML Tag এর মাধ্যমে ডেটা লেয়ারে একটি কাস্টম event পুশ করতে হবে এবং পুশ করা ইভেন্টটি ফায়ার করাতে এবং 
তা ট্র্যাকিং করে GA4 এ ইভেন্ট ডেটা পাঠাতে GTM এ কয়টি ট্যাগ ও ট্রিগার কনফিগার করতে হবে ? 
উল্ল্যেখ্য যে উক্ত পেইজ এ বাটন ক্লিক এর কোন listener নাই।
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>Response From ChatGPT
✅ মোট প্রয়োজনীয় Tag ও Trigger:
Component	    Quantity	        Purpose
Tags	                2	              Custom HTML (push),   GA4 Event Tag
Triggers	            2	              Click Trigger,              Custom Event Trigger


===============================Q2ChatGPT
একটি html পেইজ বানিয়ে দাও যেটাতে ১টি করে ইনপুট বক্স, চেক্স, অপশন রেডিও বাটন এবং বাটন থাকবে।
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>Response From ChatGPT
ChatGPT প্রদত্ত html পেইজটি হলো ঃ GA4-C03-Inject-Custom-Event-Using-GTM.html


===============================Q2ChatGPT
উপরোক্ত html পেইজকে বিবেচনায় রেখে GTM এর জন্য এমন একটি Custom HTML Tag এর কোড দাও যার মাধ্যমে উপরোক্ত পেইজ এর বাটনটি ক্লিক করলে 
অন্যান্য element গুলোর value সমুহ একটি কাস্টম ইভেন্ট এর প্যারামিটার হিসাবে ডেটা লেয়ারে পুশ করবে।
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>Response From ChatGPT
✅ Custom HTML Tag Code (GTM এ বসানোর জন্য):
<script>
  document.addEventListener('DOMContentLoaded', function () {
    var button = document.getElementById('submitBtn');
    if (button) {
      button.addEventListener('click', function () {
        // ইনপুট বক্স থেকে নাম সংগ্রহ
        var username = document.getElementById('username')?.value || '';

        // চেকবক্স থেকে সাবস্ক্রিপশন অবস্থা
        var subscribe = document.getElementById('subscribe')?.checked ? 'yes' : 'no';

        // রেডিও বাটন থেকে নির্বাচিত লিঙ্গ
        var genderRadios = document.getElementsByName('gender');
        var gender = '';
        for (var i = 0; i < genderRadios.length; i++) {
          if (genderRadios[i].checked) {
            gender = genderRadios[i].value;
            break;
          }
        }

        // Data Layer এ পুশ করা
        window.dataLayer = window.dataLayer || [];
        window.dataLayer.push({
          event: 'custom_form_submit',
          formData: {
            name: username,
            subscribe: subscribe,
            gender: gender
          }
        });

        console.log("Custom Event Fired: custom_form_submit");
        console.log({ name: username, subscribe: subscribe, gender: gender });
      });
    }
  });
</script>

=======================অথবা=======================

<script>
  document.addEventListener("DOMContentLoaded", function () {
    document.getElementById("submitBtn").addEventListener("click", function () {
      // Input values collect করা হচ্ছে
      var username = document.getElementById("username").value;
      var isSubscribed = document.getElementById("subscribe").checked;
      var genderElements = document.getElementsByName("gender");
      var genderValue = "";
      for (var i = 0; i < genderElements.length; i++) {
        if (genderElements[i].checked) {
          genderValue = genderElements[i].value;
          break;
        }
      }

      // Data Layer এ push করা হচ্ছে
      window.dataLayer = window.dataLayer || [];
      window.dataLayer.push({
        event: "formSubmit",
        userData: {
          name: username,
          subscribed: isSubscribed,
          gender: genderValue
        }
      });

      console.log("Form data pushed to dataLayer.");
    });
  });
</script>

✅ GTM Configuration Steps:
GTM এ যাও।
একটি New Tag তৈরি করো:
Tag Type: Custom HTML
HTML এর ঘরে উপরের কোডটি বসাও।
Trigger:
Trigger হিসেবে All Pages অথবা নির্দিষ্ট পেইজের জন্য Page View ব্যবহার করো যাতে কোডটি লোড হয়।
Publish করো।


✅ GTM Config এ পরবর্তী ধাপ:
🔹 Trigger for this tag:
Trigger Type: Page View (All Pages or specific page as needed)

কারণ: আমরা বাটনের জন্য listener অ্যাড করছি JavaScript দিয়ে, তাই এটা পেজ লোডেই অ্যাক্টিভ হতে হবে।

🔹 GA4 Event Tag:
Event Name: custom_form_submit

Trigger: Custom Event Trigger

Trigger Type: Custom Event

Event Name: custom_form_submit

🔹 GA4 Tag এর Event Parameters:
formData.name      --> {{formData.name}}
formData.subscribe --> {{formData.subscribe}}
formData.gender    --> {{formData.gender}}
